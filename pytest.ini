[pytest]
testpaths = tests
# Manual scripts in tests/ are named manual_* precisely so they are NOT collected:
# they drive live endpoints (and capture_lockseat.py drives a real browser). This
# pattern keeps the default `test_*` collection from ever picking them up.
python_files = test_*.py
addopts = -q --strict-markers
